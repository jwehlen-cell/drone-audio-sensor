# Backend Services

Currently contains:

- [`gateway/`](gateway/) — Python asyncio gRPC server that accepts phone streams (Session 2)

Future:

- `inference/` — YAMNet inference worker (Session 3)
- `tak_publisher/` — CoT XML publisher to TAK Server (Session 4)
