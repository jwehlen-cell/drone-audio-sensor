click==8.1.7
google-cloud-firestore==2.18.0
cryptography==43.0.0
redis[hiredis]==5.0.7
# Only used by `simulate_soh_phones.py --audio-burst` to stream synthetic
# PCM16 frames at the gateway. Heartbeat-only mode does not need these.
grpcio>=1.68
grpcio-tools>=1.68
numpy>=1.26,<3
protobuf>=4.25,<6
# Real-WAV streaming for `--audio-burst`. The sim VM's startup script
# clones the upstream WAV repos under data/sim_audio_fixtures/, and the
# simulator reads + resamples them per cycle.
soundfile>=0.12
scipy>=1.11
# (soundfile above also powers the AudioFrame.codec="flac" producer
# path. PCM16 frames are FLAC-encoded before the gRPC send; the
# inference worker decodes before YAMNet. ~1.3x bandwidth reduction
# on drone-rotor audio with ~3 ms per-frame encode CPU.)
# Argos UAT pull bridge (scripts/argos_bridge/). Reads from prod argos
# GCS (cross-project, sandbox-only) and loads test-only PKI material
# out of argosuat Secret Manager. PyJWT is staged for when
# GATEWAY_REQUIRE_AUTH gets flipped on; the bridge currently doesn't
# sign.
google-cloud-storage>=2.16
google-cloud-secret-manager>=2.20
PyJWT>=2.8
