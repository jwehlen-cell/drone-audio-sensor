click==8.1.7
google-cloud-firestore==2.18.0
cryptography==43.0.0
redis[hiredis]==5.0.7
# Only used by `simulate_soh_phones.py --audio-burst` to stream synthetic
# PCM16 frames at the gateway. Heartbeat-only mode does not need these.
grpcio>=1.68
grpcio-tools>=1.68
numpy>=1.26,<3
protobuf>=4.25,<6
